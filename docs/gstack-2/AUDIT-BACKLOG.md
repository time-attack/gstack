# GStack 2 audit backlog (P2 / FYI, not fixed this wave)

Compiled 2026-07-30 from the gstack2-integration full audit (156 findings retained after excluding items fixed by this wave, make-pdf content cuts (frozen), and the user-driven APPLE-RELEASE.md removal). Ranked P2 before FYI within each theme.

## Bloat & dead weight (42)

| Sev | Area | Location | Summary | Suggested fix |
|---|---|---|---|---|
| P2 | skills/plan/ end-to-end audit (dispatcher laziness, size, modes, bloat, hardcoding) | `skills/plan/references/legacy/retro.md:703` | retro.md global mode tells installed users 'Run `bun run build` in the gstack directory' — obsolete 1.x contributor scaffolding; standard-installer users have no gstack checkout, so the recovery instruction is a dead end. | Overlay the failure message to point at the managed runtime path (gstack doctor / re-run the runtime bundle install), not a source checkout; also dedupe the fallback chain above it (lines 695-698: checks 1, 2, and 4 test the identical $GSTACK_BIN path). |
| P2 | skills/qa (dispatcher + references + legacy + support tree) | `skills/qa/references/support/scripts/question-registry.ts:1` | ~43KB of orphaned support scaffolding in skills/qa (ETHOS.md 7.9KB, question-registry.ts 23.3KB, jargon-list.json, askuserquestion-cjk/split.md 10KB) is referenced only by the ASSETS.md relocation table — no module ever reads it — and is duplicated byte-identical into all 5 dispatchers (~215KB repo-wide), contradicting BLOAT-LEDGER's doNotCut claim that ETHOS is a load-bearing stub. | Change scripts/gstack2/generate-skill-tree.ts to emit these files only into packages whose modules actually reference them (in qa: none), and drop the corresponding ASSETS.md rows; zero context cost today but it is exactly the 'obsolete scaffolding' class GStack 2 bans. |
| P2 | skills/ship (GStack 2 ship dispatcher end-to-end audit) | `skills/ship/references/support/scripts/question-registry.ts:1` | ~43KB of orphaned 1.x scaffolding ships in every ship install, reachable only via the ASSETS.md manifest, never from any workflow prose: question-registry.ts (23,271B), askuserquestion-split.md (8,698B), ETHOS.md (7,892B), jargon-list.json (1,796B), askuserquestion-cjk.md (1,330B); question-registry/askuserquestion docs/jargon-list are referenced by NO prose in ANY of the 5 dispatchers (ETHOS only in plan). | Change scripts/gstack2/generate-skill-tree.ts to emit support assets per-skill only when a module in that skill references them (keep ETHOS in plan only), and drop the corresponding ASSETS.md rows; zero context cost today but it is dead packaging weight and misleading provenance. |
| P2 | skills/ship (GStack 2 ship dispatcher end-to-end audit) | `skills/ship/references/EXTERNAL-EFFECTS.md:1` | SKILL.md step 9 unconditionally routes every push/PR/merge/deploy through the `gstack state` durable-effect wrapper, but EXTERNAL-EFFECTS.md never states a runtime-absent fallback — a skills-only install (no optional bin/gstack runtime, the canonical `npx skills add` path) hits `gstack state begin ship` failure at the first push, relying on a generic degrade rule buried in RUNTIME.md. | Add one sentence to EXTERNAL-EFFECTS.md: if the managed runtime is absent, note it once and execute the already-authorized action host-native without the wrapper (matches the contract 'pure judgment must work without bin/gstack'). |
| P2 | skills/make-pdf end-to-end audit (size, laziness, drift, bloat, hardcoding) | `skills/make-pdf/SKILL.md:108` | SKILL.md refers to '/diagram' as the companion skill for authoring diagrams, but the canonical tree ships exactly six skills (five dispatchers + make-pdf) and no /diagram skill or compat/ routing exists. | Either reword to reference the diagram runtime capability/tool instead of a '/diagram' skill, or ship the routing it names; as written a standard install hits a dead reference. |
| P2 | Proportionality audit: /plan, /qa, /review context cost on trivial asks (gstack2-integration) | `skills/qa/references/legacy/qa.md:1` | qa legacy modules (qa.md 38.9KB, qa-only.md 21KB) carry no #886 proportionality port and zero scale/question-budget language, even though the plan-side budget claims to bind 'the entire invocation and everything it chains into'; a /plan session-scale handoff into /qa lands in modules with no budget inheritance beyond generic SHARED-JUDGMENT #10-11. | Port the #886 scale/budget overlay to qa.md and qa-only.md (generator anchor GSTACK2_FIX_886) so chained QA respects the inherited scale and remaining question budget; qa-only's Quick/diff-aware modes already give it the execution levers. |
| P2 | Proportionality audit: /plan, /qa, /review context cost on trivial asks (gstack2-integration) | `skills/plan/references/EXECUTION-PROFILES.md:9` | The forced triad (EXECUTION-PROFILES + SHARED-JUDGMENT + AUTHORITY-POLICY, ~1.7K tokens) plus CODE-INTELLIGENCE read+suggest-probe runs on every repo-targeted invocation regardless of ask size, and even the Smoke/readiness Depth says 'every selected specialist module remains mandatory' — Depth narrows claims/evidence but never reduces reading. | BLOAT-LEDGER already names the triad as 'biggest lever' but defers it; keep the deferral honest by adding module-read cost to the A/B sweep so lane-specific triads and a readiness-profile reduced-read path get measured, not just eager SKILL.md bytes. |
| P2 | size-claim verification: GStack 2 (skills/) vs GStack 1 skill surface + per-invocation context cost | `skills/plan/references/support/runtime-bootstrap.mjs:1` | 729,887 B (30.1%) of skills/ is byte-identical duplication across the six skill dirs: runtime-bootstrap.mjs 35,171 B x6, question-registry.ts 23,271 B x5, BROWSER-PROVIDERS.md 13,525 B x6, RUNTIME.md x6, redact-patterns.ts x2, StateServer.swift.template x2. | If duplication is deliberate (each skill self-contained for `skills add --skill <name>` installs), record that trade in BLOAT-LEDGER's doNotCut with the 730 KB number; it is lazy content so zero context cost, but it is the single reason the on-disk claim fails and auditors will keep rediscovering it. |
| P2 | eager-context-injection | `skills/plan/SKILL.md:31` | Forced triad (EXECUTION-PROFILES + SHARED-JUDGMENT + AUTHORITY-POLICY, 6,796 B ~= 1,699 tokens) is read eagerly on every invocation of all five dispatchers — the single largest recurring eager cost, already flagged as biggestLever in BLOAT-LEDGER but still fully eager. | Run the deferred A/B sweep and make the triad conditional/lane-specific (debug/review pilots suggest it may not be load-bearing there); do not cut without A/B win-rate evidence per the ledger. |
| P2 | eager-context-injection | `skills/plan/SKILL.md:31` | CODE-INTELLIGENCE.md (2,357 B ~= 589 tokens) is force-read once per repo-target invocation, but its body is only actionable when `gstack-code-intelligence suggest` returns offer:true (large repo, no prior decision) — for small repos and repos with a persisted decision the read is pure waste, and BLOAT-LEDGER's eager formula (SKILL.md + triad) undercounts it. | Inline the one-line `suggest --json` command into dispatch step 4 and read references/CODE-INTELLIGENCE.md only when the result says offer:true; add the ~589 tokens to the ledger's eagerPerInvocation numbers until then. |
| P2 | eager-context-injection | `skills/plan/references/legacy/sync-gbrain.md:330` | The GBrain Search Guidance block that sync-gbrain writes verbatim into a project's CLAUDE.md is 2,868 B (~717 tokens) injected into EVERY session of a synced repo — 2x the entire six-skill discovery budget — and roughly half is sync-time operational detail (autopilot race #1734, --allow-reclone, version-gated flags, schema-pack WARN) irrelevant to search routing. | Shrink the verbatim block to the routing table (prefer-gbrain-when list + pin note, ~350 tokens) and move sync/graph-build/safety detail to a lazy reference the agent reads only when running the sync module. |
| P2 | compat-purity-and-laziness | `skills/plan/references/COMPATIBILITY.md:1` | 106 files under skills/ and docs/gstack-2 carry '<!-- GENERATED by scripts/gstack2/generate-skill-tree.ts; do not edit. -->' markers pointing at the deleted generator — the files are now the only source of truth yet forbid editing, guaranteeing maintainer confusion | Strip or replace the marker (e.g. 'canonical static source, edit here') in one mechanical pass across skills/*/references/ and skills/.compat/ |
| P2 | GStack 2 unasked-features audit: skills/ + skills/.compat/ surface traced against SKILL-MIGRATION.md, BACKLOG-MAP.json, and the CLAUDE.md canonical contract | `skills/plan/references/SHARED-JUDGMENT.md:1` | 105 files under skills/ open with '<!-- GENERATED by scripts/gstack2/generate-skill-tree.ts; do not edit. -->' but that generator is deleted — maintainers are forbidden to edit files that now have no other source of truth | One pass to update or remove the marker (e.g. 'static canonical source since <sha>; edit here') so the tree has an honest maintenance story |
| P2 | GStack 2 unasked-features audit: skills/ + skills/.compat/ surface traced against SKILL-MIGRATION.md, BACKLOG-MAP.json, and the CLAUDE.md canonical contract | `skills/plan/references/EXECUTION-PROFILES.md:1` | The Depth mechanism (readiness/standard/deep via EXECUTION-PROFILES.md) plus the forced EXECUTION-PROFILES+SHARED-JUDGMENT+AUTHORITY-POLICY triad (~1,699 tokens read on every invocation of every dispatcher) traces to none of the three sources; it is the single largest recurring context cost in the v2 surface | BLOAT-LEDGER.json already flags making the triad conditional as the biggest lever, deferred to A/B — hold that line (no growth), add the missing trace entry, and run the A/B sweep before GA rather than after |
| P2 | bin/ and lib/ unasked-features audit (GStack 2 canonical contract trace) | `bin/gstack-codex-session-import:1` | bin/gstack-codex-session-import (7.8KB) has zero production invokers: no SKILL.md, no hosts/ hook, no runtime/install.js entry, no scripts/ caller — only its own unit test and a fixture copy at test/skill-e2e-plan-tune-cathedral.test.ts:343. | Delete bin/gstack-codex-session-import plus test/gstack-codex-session-import.test.ts, its touchfiles entry, and the fixture copy in the plan-tune-cathedral e2e test. |
| P2 | bin/ and lib/ unasked-features audit (GStack 2 canonical contract trace) | `bin/gstack-timeline-read:1` | bin/gstack-timeline-read (3.1KB) is the never-used read side of the timeline: not in runtime/install.js, referenced by no SKILL.md anywhere (context-restore/SKILL.md:631 reads timeline.jsonl directly via tail/grep), only tests and design docs mention it. | Delete bin/gstack-timeline-read and its cases in test/timeline.test.ts / test/gstack-project-state-worktree.test.ts; consumers already read the JSONL file directly. |
| P2 | bin/ and lib/ unasked-features audit (GStack 2 canonical contract trace) | `bin/gstack-brain-consumer:1` | bin/gstack-brain-consumer (5.9KB) and its gstack-brain-reader symlink have zero invokers in code or skills (only docs/gbrain-sync.md mentions reader) — the enqueue side is live (called by learnings-log, review-log, decision-log, timeline-log, brain-sync) but nothing ever consumes via this CLI. | Delete bin/gstack-brain-consumer and the bin/gstack-brain-reader symlink; update the stale mention in docs/gbrain-sync.md. |
| P2 | bin/ and lib/ unasked-features audit (GStack 2 canonical contract trace) | `bin/gstack-platform-detect:1` | bin/gstack-platform-detect (1KB) — multi-host agent-detection table with zero invokers (only docs/designs/SLATE_HOST.md references it); host detection/placement is explicitly owned by standard installers under the GStack 2 contract. | Delete bin/gstack-platform-detect; it is 1.x multi-host scaffolding the canonical install path forbids re-growing. |
| P2 | bin/ and lib/ unasked-features audit (GStack 2 canonical contract trace) | `bin/gstack-specialist-stats:1` | bin/gstack-specialist-stats (2.3KB) is referenced only by the retained 1.x review/SKILL.md and ship/sections/review-army.md; the canonical carve dropped every reference (0 hits in skills/) and it is not in the runtime bundle. | Delete bin/gstack-specialist-stats together with its 1.x references when the retained tree is pruned; it is already dead in the GStack 2 distribution. |
| P2 | bin/ and lib/ unasked-features audit (GStack 2 canonical contract trace) | `bin/gstack-brain-restore:1` | bin/gstack-brain-restore + bin/gstack-brain-uninstall (13.7KB pair) are 1.x-only: zero canonical skills/ references, absent from runtime/install.js, and brain-uninstall's only code caller is brain-restore itself (rest are 1.x SKILL.md preambles: pair-agent, plan-tune, autoplan, benchmark). | Delete both when the 1.x skill tree is pruned; nothing in the GStack 2 surface can reach them. |
| FYI | skills/plan/ end-to-end audit (dispatcher laziness, size, modes, bloat, hardcoding) | `skills/plan/SKILL.md:4` | Frontmatter description names the internal alias 'autoplan' in the public trigger string; the mode table itself is clean (exactly six modes, alias table explicitly marked internal, no leak as a mode). | Acceptable as a discoverability aid for 1.x users; if you want zero alias surface, say 'full planning chain' instead of 'autoplan' in the description. |
| FYI | skills/plan/ end-to-end audit (dispatcher laziness, size, modes, bloat, hardcoding) | `skills/plan/references/legacy/office-hours.md:696` | A session-scale toy ask still pays a full ~12.6K-token read of office-hours.md (Discovery's mandatory module) — the #886 proportionality port caps questions (5 at session) and skips phases but not the module read itself; this is contract-bound (legacy modules are do-not-cut) and the full chain is NOT dragged in (autoplan + 3 reviews load only in Full chain mode). | No action now; BLOAT-LEDGER already defers eager-cost levers to post-A/B. If a cheaper toy path is ever wanted, it must be a new overlay/carve through gen:gstack2, never a summary of the module. |
| FYI | skills/plan/ end-to-end audit (dispatcher laziness, size, modes, bloat, hardcoding) | `skills/plan/references/support/runtime-bootstrap.mjs:1` | references/support (132KB incl. 35KB runtime-bootstrap.mjs) plus BROWSER-PROVIDERS.md (13.5KB) are byte-identical copies across all six installed skills — disk duplication only, zero context cost (scripts are executed via node, never Read; BROWSER-PROVIDERS is reachable only lazily via RUNTIME.md/THIRD-PARTY-ACTIONS.md). | Leave as-is; standalone-per-skill install is the canonical contract ('installs with the same canonical tree'). |
| FYI | skills/plan/ end-to-end audit (dispatcher laziness, size, modes, bloat, hardcoding) | `skills/plan/SKILL.md:37` | Generator overlay-append artifacts in SKILL.md: dispatch step 9 sits detached after a blank line below steps 1-8, and the 'Global Context search is deprecated' bullet floats below the Hard rules list — cosmetic only. | Optionally have generate-skill-tree.ts merge appended overlay items into the parent list; purely cosmetic, no behavior impact. |
| FYI | skills/qa (dispatcher + references + legacy + support tree) | `skills/qa/SKILL.md:30` | The forced per-invocation triad (EXECUTION-PROFILES + SHARED-JUDGMENT + AUTHORITY-POLICY, 6,796 bytes ≈ 1,699 tokens) loads even for trivial asks; BLOAT-LEDGER already tracks making it lane-specific as the 'biggest lever', deferred pending A/B evidence. | No action now; keep the deferred A/B item alive rather than re-litigating here. |
| FYI | skills/ship (GStack 2 ship dispatcher end-to-end audit) | `skills/ship/SKILL.md:1` | Laziness verified end-to-end: SKILL.md 6,467B (~1,617 tokens); mandatory eager set (SKILL.md + EXECUTION-PROFILES 3,191B + SHARED-JUDGMENT 2,201B + AUTHORITY-POLICY 1,404B) = 13,263B ≈ 3,316 tokens, matching BLOAT-LEDGER's ship=3316; all judgment sits in references/legacy/ (58KB land-and-deploy, 38KB ship) read only when a mode activates, with ship.md's 8 heavy phases further carved into lazy references/sections/ship/* reads — nothing inlined into SKILL.md. | No action; keep the skill-size-budget tests pinning generator-preamble growth. |
| FYI | skills/make-pdf end-to-end audit (size, laziness, drift, bloat, hardcoding) | `skills/make-pdf/SKILL.md:34` | Repo-internal and templating residue in user-facing prose: 'CI and Docker builds install it automatically via Dockerfile.ci' (line 34) is meaningless in an installed skill, and line 38 has a mangled substitution ('empty boxes (▯). the explicitly approved pdf runtime capability attempts to install...') starting a sentence lowercase. | Cosmetic cleanup for the next regen pass only; no functional impact. |
| FYI | GStack 2 always-loaded discovery surface (six skills' frontmatter) + ceiling enforcement | `skills/plan/SKILL.md:1` | Frontmatter hygiene is good: all six SKILL.md files carry only name + description (no allowed-tools or extra keys), and the five dispatcher descriptions are 34-47 tokens each — no bloat findings on the surface itself. | No change needed; keep descriptions at this size and let the restored test pin them. |
| FYI | Proportionality audit: /plan, /qa, /review context cost on trivial asks (gstack2-integration) | `skills/plan/SKILL.md:41` | Positive: real proportionality machinery exists and works — plan's empty-target fast path (ls -A before any reference read), the fifteen-vector Scale classification with time-constraint ceiling and chain-wide question budget, qa-only's Quick/diff-aware modes, and #886 ports in all six plan specialists plus review.md. | No action; keep these as the pattern for the reading-side fixes above (fast paths in dispatchers, scale conditions in overlays, lazy sections for step machinery). |
| FYI | size-claim verification: GStack 2 (skills/) vs GStack 1 skill surface + per-invocation context cost | `skills/.compat/office-hours/SKILL.md:1` | CLAUDE.md contract says compatibility files live under compat/, but compat/ was deleted in 3448905e and 44 alias stubs (27,180 B) now live dot-hidden at skills/.compat/. | Update the CLAUDE.md contract sentence to the actual location (skills/.compat/) or move the stubs back; STATUS.md's 'aliases outside default five-skill discovery' claim still holds since dot-dirs are skipped by installer discovery. |
| FYI | host-coverage/claude | `skills/qa/SKILL.md:1` | Offline verification of the claude-facing skill surface PASSES: all six SKILL.md files have valid frontmatter (name==dir, description present), every referenced references/-relative path resolves, legacy modules present (debug 6, plan 14, qa 14, review 5, ship 11, make-pdf 0), and the 44 hidden skills/.compat aliases are not surfaced by CLI discovery. | No action on the skill tree itself; cite this static result plus the fresh CLI 1.5.21 discovery/install run in the refreshed matrix artifact rather than re-deriving it. |
| FYI | host-coverage/claude | `hosts/claude/hooks/question-preference-hook.ts:1` | hosts/claude/hooks (three shell wrappers + TS: auq-error-fallback, question-log, question-preference) is claude-host-specific 1.x plan-tune machinery referenced only by tests/touchfiles and not wired by ./setup or the canonical install — inert for gate (a), but it is host-specific logic sitting outside the 2.0 contract now that its generator apparatus was deleted. | Decide its disposition in the apparatus-removal cleanup: keep it clearly on the retained-1.x side or delete it with its orphaned tests; it must not become an implicit claude install step. |
| FYI | complaint-coverage audit, slice 2/10 (BACKLOG-MAP index % 10 == 2) | `CHANGELOG.md:7740` | garrytan/gstack#432 (FIX_IN_GSTACK_2, idx 42) was resolved by removing BROWSE_EXTENSIONS_DIR from browse/src entirely, but CHANGELOG.md:7740 and docs/designs/GSTACK_BROWSER_V0.md:321 still advertise the env var as SHIPPED | Note the removal in the next release entry and mark the design-doc row as removed |
| FYI | complaint-coverage slice 7/10 (BACKLOG-MAP index % 10 == 7, 25 of 75 items sampled) | `skills/plan/references/legacy/office-hours.md:753` | garrytan/gstack#538 disposition understates reality: labeled NEEDS_EVIDENCE but the founder-resources opt-out fix is already implemented and anchored (GSTACK2_FIX_538_FOUNDER_RESOURCES_OPTOUT) | Re-disposition #538 to reflect the shipped fix so future triage does not re-litigate it |
| FYI | compat-purity-and-laziness | `skills/.compat/office-hours/SKILL.md:11` | Compat purity PASSES: all 44 skills/.compat/*/SKILL.md are a uniform 15-line template, every dispatch line routes to one of the five dispatchers ($plan/$qa/$debug/$review/$ship) with a mode and module that exist in that dispatcher's mode table and references/legacy/, all carry the 'Do not reproduce or summarize the retired specialist' clause, and zero judgment keywords (checklist/rubric/phase/question) appear | No action; keep the template uniform if new aliases are ever added |
| FYI | compat-purity-and-laziness | `skills/.compat/office-hours/SKILL.md:6` | Discovery-count PASSES with skills CLI 1.5.21 (verified against cached CLI source): from skills/ the walker adds the six SKILL.md dirs and never descends into .compat (walkDeep=false at searchPath), and even fullDepth discovery drops every alias because parseSkillMd returns null for metadata.internal:true unless INSTALL_INTERNAL_SKILLS=1 — npx skills add time-attack/gstack/skills yields exactly plan, qa, debug, review, ship, make-pdf | No action; note the six-count now depends on the CLI's metadata.internal gate, worth one line in HOST-COMPATIBILITY.md when fixing its five-name list |
| FYI | compat-purity-and-laziness | `skills/plan/SKILL.md:29` | Laziness PASSES: dispatchers read only active legacy modules (protocol step 3) plus three small mandatory refs per invocation (EXECUTION-PROFILES 3.2KB + SHARED-JUDGMENT 2.2KB + AUTHORITY-POLICY 1.4KB ≈ 1.7K tokens on top of the ~10KB SKILL.md); RUNTIME/WEB-CONTEXT/CODE-INTELLIGENCE/THIRD-PARTY-ACTIONS are condition-gated; make-pdf references no legacy modules (correct for a tool skill); duplicated identical legacy/support files across packages (canary.md in qa+ship, investigate.md in qa+debug, byte-identical support/*.mjs in all six) are intentional per-package self-containment, not eager context cost | No action |
| FYI | GStack 2 unasked-features audit: skills/ + skills/.compat/ surface traced against SKILL-MIGRATION.md, BACKLOG-MAP.json, and the CLAUDE.md canonical contract | `skills/plan/SKILL.md:41` | plan's Empty-target fast path (SKILL.md lines 39-41) is untraceable to the three sources; BLOAT-LEDGER already lists trimming it as needing A/B evidence | Leave as-is pending the ledger's A/B check; just add the trace so the next audit doesn't re-flag it |
| FYI | GStack 2 unasked-features audit: skills/ + skills/.compat/ surface traced against SKILL-MIGRATION.md, BACKLOG-MAP.json, and the CLAUDE.md canonical contract | `skills/plan/agents/openai.yaml:1` | agents/openai.yaml ships in all five dispatcher packages as host-specific presentation metadata; documented only in HOST-COMPATIBILITY.md, not in the three trace sources, and sits adjacent to the contract's 'no host-specific install logic in the 2.0 path' rule | Harmless (display metadata, no judgment, no install logic) — record the trace in SKILL-MIGRATION.md or drop the files if no host actually consumes them |
| FYI | GStack 2 unasked-features audit: skills/ + skills/.compat/ surface traced against SKILL-MIGRATION.md, BACKLOG-MAP.json, and the CLAUDE.md canonical contract | `skills/plan/references/AUTHORITY-POLICY.md:8` | AUTHORITY-POLICY.md forbids joining even read-only inspection commands with && / ; / pipes, while STATUS.md records the adversarial gate-classifier being fixed specifically to ACCEPT chained read-only git inspection — the shipped policy is stricter than the gate it was written for and taxes every real session with one-command-per-tool-call overhead | Align the policy with the fixed classifier (allow read-only chains, block any chain containing a write) so canonical judgment isn't shaped by an obsolete eval false-positive |
| FYI | bin/ and lib/ unasked-features audit (GStack 2 canonical contract trace) | `bin/gstack-analytics:1` | bin/gstack-analytics, bin/gstack-community-dashboard, bin/gstack-security-dashboard (19.5KB total) are invoked by nothing in code (only each other and their tests) and are NOT bundled by runtime/install.js — yet README.md:604 promises 'Local analytics are always available. Run gstack-analytics' and ARCHITECTURE.md:255 documents the security dashboard, so installed GStack 2 users get a documented feature that doesn't exist on their machine. | Decide once: either add the three dashboards to the runtime bundle to honor the README/ARCHITECTURE claims, or delete them and fix README.md:604 and ARCHITECTURE.md:255 — the current state is a stale privacy-story promise. |
| FYI | bin/ and lib/ unasked-features audit (GStack 2 canonical contract trace) | `bin/gstack-global-discover.ts:1` | A 61MB compiled binary sits at bin/gstack-global-discover next to the tracked 19KB .ts source; it is gitignored (verified via git check-ignore) so there is no repo-bloat risk, but dev tooling leaves it inside a tracked directory where a future `git add -f`/pattern change could commit it. | Have dev-setup emit compiled bin artifacts into an ignored dist-style directory instead of bin/ (no action needed for this audit's read-only pass). |

## Hardcoded assumptions (30)

| Sev | Area | Location | Summary | Suggested fix |
|---|---|---|---|---|
| P2 | skills/debug end-to-end audit (size, laziness, bloat, scaffolding, hardcoded assumptions) | `skills/debug/references/legacy/guard.md:25` | careful/freeze/guard/unfreeze modules describe hook enforcement that cannot exist in a canonical install ('every bash command will be checked', edits 'will be blocked', guard's dependency note citing sibling skill dirs 'installed together by the gstack setup script') and write $GSTACK_HOME/freeze-dir.txt state that nothing reads — SKILL.md hard-rule 4 downgrades this to advisory, but the modules still stage state and cite scripts that don't ship. | Overlay the four modules to drop the setup-script dependency note and the no-consumer freeze-dir.txt writes when no host hook is confirmed (advisory-only path), keeping the safety judgment; also removes the redundant eval "$($GSTACK_BIN/gstack-paths)" calls that error without the optional runtime. |
| P2 | hardcoded-solutions in skills/ and compat (skills/.compat) | `skills/ship/references/sections/ship/plan-completion.md:205` | Dev-server detection probes a hardcoded port list (3000/8080/5173/4000), silently skipping plan verification for any project on another port. | Check CLAUDE.md / plan file for a dev-server URL first; keep the four-port probe only as the documented fallback. |
| P2 | hardcoded-solutions audit (bin/, lib/, skills/ production paths) | `bin/gstack-distill-free-text:202` | Hardcoded model ID claude-haiku-4-5-20251001 plus hand-copied per-token pricing constants (lines 198-200) that silently drift from real pricing | Read model from a GSTACK_DISTILL_MODEL env/config with the current ID as fallback; drop or label the pricing estimate as approximate |
| P2 | hardcoded-solutions audit (bin/, lib/, skills/ production paths) | `bin/gstack-diff-scope:7` | BASE defaults to literal "main" with no branch detection (master/trunk repos silently get empty-diff SCOPE_*=false output) | Fall back to origin/HEAD detection (git symbolic-ref refs/remotes/origin/HEAD) before defaulting to main |
| P2 | hardcoded-solutions audit (bin/, lib/, skills/ production paths) | `bin/gstack-next-version:408` | --base falls back to literal "main" without detecting the repo default branch | Reuse the origin/HEAD → main/master detection already implemented in bin/gstack-redact-prepush:74-80 |
| P2 | Hardcoded solutions + obsolete scaffolding in scripts/, bin/, package.json | `bin/gstack-distill-free-text:202` | Hardcoded dated model ID claude-haiku-4-5-20251001 plus hardcoded pricing constants in a production path reachable from /plan's plan-tune legacy module | Read model from an env/config override (e.g. GSTACK_DISTILL_MODEL) with the current value as default; dated IDs get deprecated and the pin will silently 404 |
| P2 | Hardcoded solutions + obsolete scaffolding in scripts/, bin/, package.json | `scripts/declared-annotation.ts:16` | Leftover 1.x-only scripts (declared-annotation.ts, brain-cache-spec.ts, discover-skills.ts, question-registry.ts, archetypes.ts, psychographic-signals.ts ~1.7K lines) reference the deleted scripts/resolvers/ pipeline and exist only to serve their own tests | Decide whether the 1.x tmpl pipeline is retained or retired; if retired, delete these scripts and their tests together rather than keeping self-referential scaffolding |
| P2 | hardcoded-solutions audit: test/ helpers + fixtures, skills judgment surface | `test/helpers/auq-sdk-capture.ts:194` | auq-sdk-capture.ts hardcodes 'claude-opus-4-7' as default model at three sites, bypassing the resolveEvalModel() contract ('gstack never names a model') | Change `opts.model ?? 'claude-opus-4-7'` (lines 194, 256, 337) to `opts.model ?? resolveEvalModel()` and omit --model when undefined, matching session-runner/agent-sdk-runner |
| P2 | hardcoded-solutions audit: test/ helpers + fixtures, skills judgment surface | `test/helpers/claude-pty-runner.ts:443` | PTY runner warmup spawn hardcodes `--model claude-haiku-4-5`; goes stale when the alias is retired and ignores GSTACK_EVAL_MODEL pinning | Drop the --model flag (let the host default apply) or route through resolveEvalModel(); a warmup call doesn't need a pinned model |
| P2 | hardcoded-solutions audit: test/ helpers + fixtures, skills judgment surface | `test/helpers/llm-judge.ts:302` | LLM-judge hardcodes dated snapshot id 'claude-haiku-4-5-20251001' as fallback (llm-judge.ts:60) and pins it explicitly at line 302 even when GSTACK_EVAL_MODEL is set | Make line 302 use resolveEvalModel() fallback like line 60, and hoist the fallback id to one exported const so a model retirement is a one-line fix |
| P2 | complaint-coverage audit, slice 3/10 of docs/gstack-2/BACKLOG-MAP.json (items with index % 10 == 3; 25 of 76 sampled, prioritizing context-bloat/overkill/obsolete/hardcoded complaints) | `bin/gstack-repo-mode:42` | #2195 (idx 683) FIX_IN_GSTACK_2 unimplemented: bin/gstack-repo-mode:42 still uses `stat -f %m \|\| stat -c %Y`; GNU stat -f exits 0 with non-epoch output on Git Bash so the fallback never fires and the arithmetic crashes — script is referenced by many live 1.x skills (autoplan, ios-qa, cso, benchmark, pair-agent) | Use `stat -c %Y ... \|\| stat -f %m ...` order-flip plus a numeric guard (case $val in ''\|*[!0-9]*) val=0;; esac) so non-numeric stat output degrades to cache-miss instead of crashing |
| P2 | complaint-coverage audit, slice 3/10 of docs/gstack-2/BACKLOG-MAP.json (items with index % 10 == 3; 25 of 76 sampled, prioritizing context-bloat/overkill/obsolete/hardcoded complaints) | `skills/ship/references/sections/ship/pr-body.md:42` | #1079 (idx 193) FIX_IN_GSTACK_2 unimplemented: no Projects-classic fallback anywhere in the ship skill — skills/ship/references/sections/ship/pr-body.md:42 still instructs plain `gh pr edit --body-file` with no `gh api` fallback (only the document-release step warns-and-continues) | Add a one-line fallback to the PR-body update step: on gh pr edit failure, `gh api repos/{owner}/{repo}/pulls/{n} -X PATCH -f body=@file` (bypasses the deprecated GraphQL Projects-classic fields) |
| P2 | complaint-coverage audit, slice 3/10 of docs/gstack-2/BACKLOG-MAP.json (items with index % 10 == 3; 25 of 76 sampled, prioritizing context-bloat/overkill/obsolete/hardcoded complaints) | `test/gstack-gbrain-source-wireup.test.ts:235` | #2255 (idx 723) FIX_IN_GSTACK_2 unimplemented: the 'gbrain missing on PATH' strict-mode test still puts /opt/homebrew/bin on PATH (test/gstack-gbrain-source-wireup.test.ts:235 and :392), so on any dev machine with brew-installed gbrain the missing-gbrain premise is false and the test flakes | Drop /opt/homebrew/bin (and any dir that can contain a real gbrain) from the PATH in the gbrain-missing cases; keep only /usr/bin:/bin plus the empty fakeBinDir |
| P2 | complaint-coverage audit, slice 4/10 (BACKLOG-MAP items index % 10 == 4; 76 items, 24 sampled/verified) | `lib/model-benchmark/providers/claude.ts:26` | garrytan/gstack#1890 (idx 474) is mislabeled DEFER_COMMUNITY: the bug is live in retained internal tooling this very branch extends (new lib/model-benchmark/evaluation.ts, test/model-benchmark-evaluation.test.ts). available() at lib/model-benchmark/providers/claude.ts:26-30 sniffs only ~/.claude/.credentials.json or ANTHROPIC_API_KEY, so macOS Keychain-auth users get a false 'No Claude auth found'; the hardcoded os.homedir()/.claude path also ignores CLAUDE_CONFIG_DIR. | Re-disposition to FIX_IN_GSTACK_2 (or RETAIN_INTERNAL with a fix): treat a successful `claude -p` probe or Keychain presence as auth evidence instead of the credentials-file sniff, and honor CLAUDE_CONFIG_DIR. |
| P2 | GStack 2 unasked-features audit: skills/ + skills/.compat/ surface traced against SKILL-MIGRATION.md, BACKLOG-MAP.json, and the CLAUDE.md canonical contract | `skills/plan/references/THIRD-PARTY-ACTIONS.md:1` | references/THIRD-PARTY-ACTIONS.md (all 5 dispatchers, mandatory read before any third-party-site step) is untraceable to the three sources and hardcodes a single vendor ('Today that is the Aside AI browser') into canonical judgment prose | It matches a real user directive (aside-for-online-tasks), so keep the behavior but record the trace (backlog/migration entry) and keep vendor naming inside BROWSER-PROVIDERS.md detection only, so the judgment file doesn't need editing when the vendor list changes |
| FYI | skills/qa (dispatcher + references + legacy + support tree) | `skills/qa/SKILL.md:1` | Lazy-dispatcher contract verified clean: SKILL.md is 5,910 bytes / 74 lines of pure routing (no inlined judgment, no model names, no framework/test-command hardcoding); legacy 'main' branch mentions are detection fallbacks after gh-based detection, not hardcoded assumptions. | None — this is the shape the canonical contract asks for. |
| FYI | skills/debug end-to-end audit (size, laziness, bloat, scaffolding, hardcoded assumptions) | `skills/debug/SKILL.md:30` | Forced every-invocation triad costs 1,699 tokens (EXECUTION-PROFILES + SHARED-JUDGMENT + AUTHORITY-POLICY) plus CODE-INTELLIGENCE (589 tokens) whenever the target is a repository — for debug that is nearly always, so the real eager floor is ~3,560 tokens, not the ledger's 2,971. | No action now — BLOAT-LEDGER already tracks the triad as 'biggest lever, deferred to post-A/B-sweep'; just record the repo-target CODE-INTELLIGENCE add-on in the ledger's eager figures so the A/B baseline is honest. |
| FYI | skills/debug end-to-end audit (size, laziness, bloat, scaffolding, hardcoded assumptions) | `skills/debug/references/support/runtime-bootstrap.mjs:1` | ~90KB of byte-identical support files (runtime-bootstrap.mjs 35KB, browser-choice.mjs, browser-provider-smoke.mjs, contracts) are duplicated across all six skills — disk/install bloat only (zero context cost), and deliberate for standalone npx-skills installs. | Accept as the price of self-contained per-skill installs; the generator is already the single source, so no dedup work is warranted unless install size becomes a user complaint. |
| FYI | skills/make-pdf end-to-end audit (size, laziness, drift, bloat, hardcoding) | `skills/make-pdf/references/support/runtime-bootstrap.mjs:26` | Bundled bootstrap pins BOOTSTRAP_RELEASE_TAG = "v2.0.0-rc.6" (runtime-bootstrap.mjs:26), so installed skills will fetch the release-candidate manifest forever unless the file is re-emitted at final release. | Verify the release process re-pins this tag before GA; the hardcoded time-attack/gstack host allowlist itself is intentional per the signed-manifest design. |
| FYI | GStack 2 always-loaded discovery surface (six skills' frontmatter) + ceiling enforcement | `test/test-free-shards.test.ts:143` | test-free-shards.test.ts uses 'test/gstack2-skills.test.ts' as a hardcoded example filename in a buildShardArgs unit test, referencing a file that no longer exists (cosmetic, test still passes since it never touches disk). | Rename the example path when the ceiling test is restored (no-op if restored under the same name). |
| FYI | hardcoded-solutions in skills/ and compat (skills/.compat) | `skills/plan/references/support/scripts/question-registry.ts:36` | Byte-identical question-registry.ts duplicated into 5 skills' support/scripts, each telling installed-skill readers to run repo-internal `bun test test/plan-tune.test.ts` which doesn't exist in an install. | Drop or reword the contributor-only run instruction in the generator input; duplication itself is generator-owned and acceptable. |
| FYI | hardcoded-solutions in skills/ and compat (skills/.compat) | `skills/ship/references/artifacts/review/greptile-triage.md:202` | Example triage-log rows use `garrytan/myapp` as the repo slug (also in skills/review copy) — illustrative only, but a personal username baked into distributed skill content. | Swap examples to a neutral slug like `acme/myapp` in the generator source. |
| FYI | hardcoded-solutions-lib | `lib/code-intelligence/sourcebot-adapter.ts:38` | DEFAULT_URL http://localhost:3000 baked in, but overridable via opts.baseUrl and SOURCEBOT_URL env | No change needed — this is Sourcebot's documented default port and both override paths exist (line 82); keep as-is |
| FYI | hardcoded-solutions audit (bin/, lib/, skills/ production paths) | `lib/code-intelligence/sourcebot-adapter.ts:38` | Sourcebot default URL http://localhost:3000 is hardcoded but SOURCEBOT_URL env override exists and provider is opt-in per contract | Acceptable as-is; no change needed |
| FYI | hardcoded-solutions audit (bin/, lib/, skills/ production paths) | `skills/plan/references/support/runtime-bootstrap.mjs:28` | Release-manifest URLs pin time-attack/gstack + a release tag across five skills' runtime-bootstrap.mjs (generated copies) — matches canonical repo, but 5x duplication means one generator input owns it | Confirm these are generator-owned (scripts/gstack2) so a repo/tag change regenerates all copies; no manual edits |
| FYI | Hardcoded solutions + obsolete scaffolding in scripts/, bin/, package.json | `scripts/pr-test-env.ts:77` | Base branch defaults to hardcoded 'origin/main' without detection (slop-diff.ts:19 same, plain 'main') | Acceptable for repo-internal dev tools with a --base flag; if these ever run on user repos, detect via gh repo view --json defaultBranchRef |
| FYI | hardcoded-solutions audit: test/ helpers + fixtures, skills judgment surface | `bin/gstack-global-discover.ts:250` | bin/gstack-global-discover.ts decodes project slugs with a comment example baked to /Users/garrytan; code itself is generic | No code change needed; comment-only, listed to confirm it was checked |
| FYI | complaint-coverage audit, slice 3/10 of docs/gstack-2/BACKLOG-MAP.json (items with index % 10 == 3; 25 of 76 sampled, prioritizing context-bloat/overkill/obsolete/hardcoded complaints) | `docs/gstack-2/BACKLOG-MAP.json` | Six slice-3 items are stale under-claims still marked NEEDS_EVIDENCE though already resolved in-tree: #452 (review.md:784), #592 (pre-mortem in plan-eng-review.md), #703 (repo-local design docs) are ported per JUDGMENT-PROVENANCE; #1991/#2067 fixed at bin/gstack-developer-profile:238; #1389 make-pdf setup is no longer buried | Refresh BACKLOG-MAP dispositions (or add a resolvedBy field) for items superseded by landed ports/fixes so the map stays usable as a coverage ledger; no code change needed |
| FYI | complaint-coverage audit, slice 4/10 (BACKLOG-MAP items index % 10 == 4; 76 items, 24 sampled/verified) | `docs/gstack-2/BACKLOG-MAP.json:22082` | Five slice items marked NEEDS_EVIDENCE are already resolved in the current tree, so the map understates coverage of the exact complaints GStack 2 targets: #1172 (diff bumped to 9.0.0), #1968 (GSTACK_CHROMIUM_PATH honored, browse/src/browser-manager.ts:55), #2069 (single-skill install proven via --skill in STATUS.md), #2096 (installed skill surface is 5-10KB SKILL.md files, no shipped CLAUDE.md, vs the 58.5k-char bloat complaint), #2156 (make-pdf resolves browse via $GSTACK_BIN, not cwd-dependent alias). | Batch re-disposition these to SUPERSEDED_BY_CONSOLIDATION/RETAIN_INTERNAL with one-line evidence pointers; cheap map hygiene that shows the bloat/hardcoding complaints were actually killed. |
| FYI | complaint-coverage audit, slice 5/10 of docs/gstack-2/BACKLOG-MAP.json (items idx % 10 == 5) | `/Users/sinabina/gstack/docs/gstack-2/BACKLOG-MAP.json:1` | Five slice items carrying the exact complaints GStack 2 exists to kill are labeled NEEDS_EVIDENCE ('requires repro before implementation') yet are already resolved in the current tree: #2214 (autoplan 1839-line bloat → 98-line lazy dispatcher), #1102 (hardcoded Rails/Node ship test commands → ship.md:675 reads CLAUDE.md, never baked-in), #1081 (hourly GitHub update-check → no canonical skill calls it; helper fails closed without --force), #949 (/ship too expensive → lazy dispatch), #879 (invisible-content approvals → question-visibility rule at codex.md:819) | Re-disposition these to SUPERSEDED_BY_CONSOLIDATION with a pointer to the resolving artifact; stale NEEDS_EVIDENCE labels invite re-opening finished work and under-report the consolidation's actual complaint coverage |

## Egress & privacy (37)

| Sev | Area | Location | Summary | Suggested fix |
|---|---|---|---|---|
| P2 | skills/plan/ end-to-end audit (dispatcher laziness, size, modes, bloat, hardcoding) | `skills/plan/references/WEB-CONTEXT.md:6` | WEB-CONTEXT.md hardcodes Context.dev commercial terms ('currently 500 work-email or 250 personal-email monthly credits, no card') — vendor pricing baked into a generated judgment file will silently drift. | Replace the specific credit numbers with 'free tier, verify current terms' in the generator source; keep the consent/egress language unchanged (file is on the doNotCut list for consent stubs, but content accuracy fixes are in scope). |
| P2 | skills/qa (dispatcher + references + legacy + support tree) | `skills/qa/references/WEB-CONTEXT.md:6` | WEB-CONTEXT.md hardcodes Context.dev commercial terms ('500 work-email or 250 personal-email monthly credits, no card') that will silently rot; the 'verify current terms' hedge still primes the agent to quote stale numbers. | Replace the numeric credit claims with 'free tier available; verify current terms at signup' — the consent gate does not need the pricing detail. |
| P2 | skills/debug end-to-end audit (size, laziness, bloat, scaffolding, hardcoded assumptions) | `skills/debug/references/legacy/ios-fix.md:57` | ios-fix.md hardcodes gstack's own repo conventions into arbitrary user iOS projects: regression tests at test/fixtures/ios-fix/<bug-slug>.test.ts (a TypeScript test in a Swift/Xcode repo) gated by GSTACK_HAS_IOS_DEVICE=1 and 'periodic tier' — gstack-internal CI vocabulary that violates the project's own platform-agnostic rule (read CLAUDE.md, else ask). | Overlay (BUG_FIX port) the fixture/test instructions to 'use the target project's test layout from its CLAUDE.md; if none, AskUserQuestion and persist the answer', keeping the snapshot-fixture judgment intact. |
| P2 | skills/review/ (GStack 2 review dispatcher package) | `skills/review/references/support/runtime-bootstrap.mjs:1` | ~60KB of browser infrastructure (runtime-bootstrap.mjs 35KB, BROWSER-PROVIDERS.md 13.5KB, browser-choice.mjs 6.5KB, browser-provider-smoke.mjs 4.6KB) ships in the review package although zero of its five modules do browser work ($B is bound but used 0 times), duplicated byte-identically across all six skill packages (~360KB total). | Disk-only cost (never read eagerly), but the generator should include browser provider assets only in packages whose modules can actually reach interactive browser work (qa, make-pdf); keep the consent-stub prose per BLOAT-LEDGER doNotCut, trim the unreachable .mjs payloads. |
| P2 | Proportionality audit: /plan, /qa, /review context cost on trivial asks (gstack2-integration) | `docs/gstack-2/BLOAT-LEDGER.json:5` | BLOAT-LEDGER's headline ('lean by architecture, ~3-4K tokens per invocation') measures only SKILL.md + triad; the mandatory mode module (21-48KB) is counted as 'lazy preserved judgment, off-limits' even though it is read in full on every invocation of that mode, so the ledger understates the real trivial-ask floor 3-5x (review: claimed 2,981 vs ~15.7K actual). | Add a 'typical-invocation' row per dispatcher (eager + smallest mandatory module + conditional refs) so the ledger tracks the number users actually pay; the current method makes the proportionality regression invisible. |
| P2 | size-claim verification: GStack 2 (skills/) vs GStack 1 skill surface + per-invocation context cost | `test/helpers/capture-parity-baseline.ts:85` | No surviving size test sees the v2 tree: skill-size-budget.test.ts uses capture-parity-baseline.ts which scans only repo-root top-level SKILL.md dirs (the 1.x surface), so skills/*/SKILL.md and the forced triad have zero regression coverage. | Add one free gate-tier test pinning bytes of skills/*/SKILL.md + references/{EXECUTION-PROFILES,SHARED-JUDGMENT,AUTHORITY-POLICY}.md against BLOAT-LEDGER's eagerPerInvocation values (e.g. ceiling = ledger x1.1) — makes the only true half-size claim enforceable in ~30 lines. |
| P2 | ios-qa daemon egress/privacy audit (P0 gate c — retained-tool egress) | `ios-qa/daemon/src/session-tokens.ts:99` | GET /auth/sessions on the unauthenticated loopback listener (ios-qa/daemon/src/index.ts:245-247) returns full SessionToken objects including the raw token string (session-tokens.ts:99-101), so any localhost process can harvest live tailnet bearers and replay them from a remote tailnet node under the victim identity (audit-log attribution poisoning) — same pattern as the browse /health AUTH_TOKEN leak CLAUDE.md says not to worsen. | Redact tokens in list() to a prefix (token.slice(0,8) + '…'), matching how index.ts:355 already truncates tokens for the attempts log; revoke-by-token still works since callers hold their own token. |
| P2 | Egress/privacy audit: gbrain + code-intelligence plumbing (lib/, bin/gstack-*, provider contract) | `lib/code-intelligence/selection.ts:67` | Two disjoint consent stores for the same gbrain code-egress path: ~/.gstack/code-intelligence.json per-path consents vs ~/.gstack/gbrain-repo-policy.json per-remote tiers; a repo marked deny in one is still indexable via consent recorded in the other | Make hasConsent/setConsent (or cmdIndex in bin/gstack-code-intelligence) consult the repo-policy deny tier, or collapse to a single store |
| P2 | Egress/privacy audit: gbrain + code-intelligence plumbing (lib/, bin/gstack-*, provider contract) | `bin/gstack-brain-consumer:161` | bin/gstack-brain-consumer and its byte-identical 201-line duplicate bin/gstack-brain-reader (deprecated v1.17.0.0, removal promised v1.18.0.0, VERSION now 1.64.20.0) still ship a live curl POST with Bearer token to a /ingest-repo endpoint that never existed | Delete both files plus an upgrade migration — obsolete scaffolding with a dead egress sink that inflates every future audit |
| P2 | Egress/privacy audit: Context.dev client (runtime/context.js, runtime/cli.js context commands, runtime/config.js network choice) | `runtime/context.js:526` | hasContextNetworkConsent fails open on malformed validation status: `config?.context?.validation?.status !== "unverified"` passes for undefined/garbage status (hand-edited config with validation:{} enables network without any key verification); loadConfig never runs validateConfig on read, so the write-path coherence checks don't protect this. | Change the condition to `config?.context?.validation?.status === "verified"` in runtime/context.js — behavior-identical on the real write path (only setup writes verified/unverified) and fail-closed on malformed configs. |
| P2 | egress-privacy-audit: telemetry / braintrust / deepeval / eval backends | `bin/gstack-model-benchmark:165` | bin/gstack-model-benchmark contradicts the e1cd3096 fix: line 5 still says the BRAINTRUST_API_KEY 'is the consent' and the line-165 dry-run banner reports 'upload: ON' from key presence alone, yet line 124 never passes upload:true — so the banner claims egress that cannot happen and the opt-in path promised in braintrust-eval.ts:44-47 (--upload flag / braintrust_upload config) is unimplemented dead doc | Fail-safe direction (upload is currently impossible), but fix the lie: base the banner on the actual upload opt-in, update the line-5 header, and either wire a real --upload flag through to opts.upload or delete the references to it; same stale 'export BRAINTRUST_API_KEY = opt in' comment in evals/model-benchmark/gstack.eval.ts:11-13 whose line 21 also never sets upload |
| P2 | egress-privacy-audit: telemetry / braintrust / deepeval / eval backends | `.deepeval/.deepeval_telemetry.txt:1` | .deepeval/ is untracked but not gitignored; .deepeval_telemetry.txt holds a stable pseudonymous machine ID (DEEPEVAL_ID) that would be published if committed, and the artifact dir is obsolete run scaffolding that makes the tree imply default-on egress | Delete .deepeval/ and add .deepeval/ to .gitignore (outputs/ and .tmp-ios-ab-evaluator-final/ are inert report artifacts but should get the same ignore-or-delete treatment before landing on main) |
| P2 | Egress/privacy audit: setup + installer + runtime bundle (setup, bin/gstack, runtime/, retained bin helpers, gstack-upgrade/) | `runtime/runtime-bootstrap.mjs:522` | Signature verification silently degrades: verifyCosignWhenAvailable prints a note and proceeds with SHA-256-only when cosign is not installed (line 529) or when the manifest declares no cosignBundleUrl (line 524), while the browser-options text (line 105) promises 'exact signed component bytes are shown by preview before consent' — the SHA-256s come from a manifest fetched from the same origin as the artifacts, so a release-channel compromise defeats both. | Either require an explicit --allow-unsigned flag to proceed without cosign, or correct the preview/options wording to 'SHA-256-verified (Cosign-attested when cosign is installed)' and surface the degradation in the preview plan output, not just mid-install stdout. |
| P2 | Egress/privacy audit of Chrome extension (extension/) — P0 gate (c) retained-tool egress audit | `bin/gstack-extension:14` | 65-line installer script for the deleted extension always exits 1 now (checks for extension/manifest.json which no longer exists) — obsolete scaffolding that also opens chrome://extensions via osascript on a guaranteed-dead path. | Delete bin/gstack-extension. |
| P2 | Egress/privacy audit of Chrome extension (extension/) — P0 gate (c) retained-tool egress audit | `extension/lib/xterm.js:1` | 292K of orphaned untracked vendor blobs (xterm.js, xterm.css, xterm-addon-fit.js) left on disk after the removal commit because they were gitignored — verified zero network call sites (only a benign window.open() link handler and DOM .open()); the entire extension/ dir is dead weight. | rm -rf extension/ and drop the four stale .gitignore entries (extension/.auth.json at line 22, extension/lib/* at lines 24-26). |
| P2 | complaint-coverage audit, slice 0/10 of docs/gstack-2/BACKLOG-MAP.json (items at index % 10 == 0; 76 items, 21 sampled) | `docs/gstack-2/BACKLOG-MAP.json` | garrytan/gstack#1379 (idx 280, FIX_IN_GSTACK_2, headed browse fails to launch on macOS Tahoe with 'Mach rendezvous failed') has zero trace in the current tree: no fix, no regression fixture, no ARCHITECTURE/STATUS row, no commit mentioning it. | Add it to the ARCHITECTURE.md infrastructure-disposition rows or an explicit open-defects list so the FIX_IN_GSTACK_2 disposition is tracked rather than silently dropped; it blocks headed-mode users on current macOS stable. |
| P2 | complaint-coverage audit, slice 2/10 (BACKLOG-MAP index % 10 == 2) | `gstack-upgrade/migrations/v1.27.0.0.sh:133` | garrytan/gstack#1383 (FIX_IN_GSTACK_2, idx 282) unimplemented: the v1.27 migration still auto-proceeds when stdin is not a TTY ('non-interactive: proceeding automatically', v1.27.0.0.sh:133-135) — the exact complained behavior; step journaling mitigates half-done reruns but the no-TTY consent bypass for a destructive repo rename persists | Make non-interactive runs skip-and-warn (exit 0 without touching state) instead of proceeding, or require an explicit env opt-in |
| P2 | complaint-coverage audit, slice 6/10 of docs/gstack-2/BACKLOG-MAP.json (items with index % 10 == 6; 24 of 75 sampled) | `bin/gstack-telemetry-sync:32` | garrytan/gstack#2215 (FIX_IN_GSTACK_2, unimplemented): bin/gstack-telemetry-sync still exits 0 silently when supabase/config.sh is absent from the resolved GSTACK_DIR (lines 23-32), preserving the exact 'telemetry sync silently no-ops on non-Claude hosts' defect and contradicting the canonical contract's typed-failure-code requirement for external services. | Verify the GStack 2 managed runtime bundle ships supabase/config.sh next to bin/, and replace the silent exit with a typed failure code. |
| P2 | complaint-coverage audit, slice 6/10 of docs/gstack-2/BACKLOG-MAP.json (items with index % 10 == 6; 24 of 75 sampled) | `setup:1` | garrytan/gstack#1176 (FIX_IN_GSTACK_2, unverifiable): no unborn-HEAD guard exists anywhere (grep for unborn/symbolic-ref across setup, bin/, lib/, skills/ is empty) and no regression fixture references the issue, so the Conductor Quick Start failure mode has neither a fix nor the reproduction STATUS.md line 226 says BUG_FIX-class items require. | Either add an unborn-HEAD guard where git rev-parse is assumed to succeed, or re-disposition (the Conductor quick-start template path may no longer exist in GStack 2 — record that decision instead of leaving FIX_IN_GSTACK_2 open). |
| P2 | Triage of 7 untracked new test/lib files on gstack2-integration | `lib/model-benchmark/evaluation.ts:14` | lib/model-benchmark/evaluation.ts is superseded dead code: braintrust-eval.ts:81 states its deterministic scorer 'Replaces the old in-house evaluation.ts', it has zero production callers (only its own test imports it), and bin/gstack-model-benchmark has no --rubric flag using parseEvaluationSpec. | Delete evaluation.ts AND test/model-benchmark-evaluation.test.ts together (the test passes 2/2 but only pins the dead lib). Keeping them re-adds the in-house scoring the Braintrust outsourcing decision removed — textbook obsolete scaffolding. |
| P2 | Triage of 7 untracked new test/lib files on gstack2-integration | `test/platform-bakeoff.test.ts:2` | test/platform-bakeoff.test.ts passes (2/2, 61ms, no API key) but permanently re-validates a frozen synthetic research corpus (evals/platform-bakeoff/corpus.json, retained per its README 'only as reproducible research evidence, not part of the GStack runtime'), and hard-depends on that untracked dir. | Commit-optional, leaning drop: a static self-check of never-changing research evidence adds no regression protection. If evals/platform-bakeoff/ (32K) is committed as bake-off evidence, the two files must travel together and the test is gate tier; the model-name-neutrality assertion is the only piece with lasting contract value. Note the bakeoff README's `gstack-model-benchmark --rubric` example is stale (no such flag exists). |
| P2 | Untracked-directory junk audit (outputs/, .deepeval/, .tmp-ios-ab-evaluator-final/, .claude/) | — | .deepeval/ (20KB) is deepeval framework cache/telemetry (telemetry txt, cache json, latest-run json) — pure runtime junk, and .deepeval_telemetry.txt suggests deepeval telemetry may not be opted out (contract: external services off by default) | Nothing to salvage. Add `.deepeval/` to .gitignore; verify DEEPEVAL_TELEMETRY_OPT_OUT=1 is set wherever deepeval runs |
| P2 | bin/ and lib/ unasked-features audit (GStack 2 canonical contract trace) | `bin/chrome-cdp:1` | bin/chrome-cdp (2.3KB) — manual Chrome-with-CDP launcher from before the browse daemon owned browser lifecycle; zero invokers anywhere (only CHANGELOG.md and the egress-audit artifact list it; absent from setup, package.json, and every skill). | Delete bin/chrome-cdp; the browse binary/daemon is the only sanctioned browser path per the contract. |
| P2 | bin/ and lib/ unasked-features audit (GStack 2 canonical contract trace) | `bin/gstack-open-url:1` | bin/gstack-open-url (338B) — 14-line open/xdg-open wrapper with zero invokers; only a TODOS.md aspiration and the egress-audit inventory mention it. | Delete bin/gstack-open-url; any future caller can inline `open`/`xdg-open` in one line. |
| FYI | eager-context-injection | `skills/.compat:1` | Verified-clean eager surfaces, no action needed: 44 .compat aliases are dot-hidden with metadata.internal and add 0 discovery tokens; the SessionStart hook (bin/gstack-session-update) is team-mode-gated, backgrounded, and emits nothing to context; decision resurfacing and the code-intelligence offer are invocation-time, not session-start; reference-doc duplication across the six skill dirs (~46 KB x6) is disk-only, justified by selected-skill installs. | Keep as-is; these match the canonical contract (lazy dispatchers, opt-in compat, consent-gated optional services). |
| FYI | hardcoded-solutions audit: test/ helpers + fixtures, skills judgment surface | `CHANGELOG.md:467` | CHANGELOG.md leaks operator username in a transcript path (~/.claude/projects/-Users-sinabina-spacegame/...) | Reword to a machine-neutral description (the pr-886.json regression is already the durable receipt); check .claude/worktrees copies too |
| FYI | Egress/privacy audit of browse server + daemon + tunnel (browse/src/) — GStack 2 P0 gate (c) | `bin/gstack-telemetry-sync:85` | gstack-telemetry-sync strips local-only fields (_repo_slug, _branch, repo) from skill-usage.jsonl with sed regexes ([^"]*) before the Supabase upload; if a value ever contains an escaped quote the strip terminates early and leaks a fragment — community-tier opt-in only, and file is bin/ (reached from browse via security.ts spawn chain), so exposure is minimal | Replace the sed strip with a jq `del(._repo_slug, ._branch, .repo)` (jq is already assumed elsewhere) or drop the local-only fields at write time in gstack-telemetry-log for community tier |
| FYI | make-pdf egress/privacy audit (skills/make-pdf/ + make-pdf/src/) — feeds STATUS.md P0 gate (c) | `skills/make-pdf/SKILL.md:38` | SKILL.md:38 documents a Linux emoji-font install (apt/dnf/pacman/apk → distro mirrors) performed by the approved `pdf` runtime capability; the installer code ships only in the signed release artifacts, so this egress site is not auditable from the repo skill tree (consent gating rests on RUNTIME.md's --yes install flow plus GSTACK_SKIP_FONTS opt-out). | For gate (c) evidence, cite the runtime-artifact installer source (or its release provenance doc) so the font-install egress has an auditable code reference, not just prose. |
| FYI | ios-qa daemon egress/privacy audit (P0 gate c — retained-tool egress) | `ios-qa/daemon/src/index.ts:128` | GSTACK_IOS_TAILNET_BIND (ios-qa/daemon/src/index.ts:128) accepts any bind address with no validation that it is a Tailscale interface; mint stays fail-closed (WhoIs fails for non-tailnet peers) but bearer-authenticated endpoints become reachable from whatever network the operator binds to. | Validate the bind address against tailscaled's own interface addresses (available via the LocalAPI already in use) or document the env var as expert-only in SKILL.md. |
| FYI | ios-qa daemon egress/privacy audit (P0 gate c — retained-tool egress) | `ios-qa/scripts/gen-accessors-tool/Package.swift:23` | Dev-time only: gen-accessors Swift tool declares swift-syntax from github.com with a version range (from: 510.0.0), so SPM fetches from GitHub at build time without an exact pin (ios-qa/scripts/gen-accessors-tool/Package.swift:23); this never runs in the QA runtime path. | Acceptable as-is (standard SPM dev dependency); pin exact if supply-chain pinning becomes a repo-wide policy. |
| FYI | Egress/privacy audit: gbrain + code-intelligence plumbing (lib/, bin/gstack-*, provider contract) | `lib/code-intelligence/sourcebot-adapter.ts:136` | Sourcebot search/status (and the detectAvailable liveness probe fired by options/status/suggest) send the query string plus Bearer SOURCEBOT_API_KEY to a non-loopback SOURCEBOT_URL without the egress consent gate — consent covers only register_source/refresh | Accept as query-only egress to a user-configured host but document it in PRIVACY.md, or extend assertEgressConsent to non-loopback search |
| FYI | Egress/privacy audit: Context.dev client (runtime/context.js, runtime/cli.js context commands, runtime/config.js network choice) | `runtime/doctor.js:64` | doctor.js reports 'Context.dev network mode has explicit consent' using only 2 of the 4 gate conditions (mode+consent, omitting selection and validation), so doctor can show a green network check while every client operation would still throw CONTEXT_BLOCKED (e.g. after `setup --offline`). Display-only drift, no egress impact. | Reuse/export hasContextNetworkConsent from runtime/context.js in the doctor check instead of a second inline definition, so the displayed state matches the actual gate. |
| FYI | Egress/privacy audit: Context.dev client (runtime/context.js, runtime/cli.js context commands, runtime/config.js network choice) | `runtime/context.js:147` | readContextKey falls back to the generic env var CONTEXT_API_KEY (in addition to CONTEXT_DEV_API_KEY); once consent is on, an unrelated tool's ambient 'context' credential would be sent as Bearer to api.context.dev, and the env key silently shadows the verified key in secrets.json. Low impact: TLS to pinned official host only, typed 401 → CONTEXT_KEY_INVALID. | Drop the generic CONTEXT_API_KEY alias or log the key source on use; keep only the unambiguous CONTEXT_DEV_API_KEY. |
| FYI | egress-privacy-audit: telemetry / braintrust / deepeval / eval backends | `bin/gstack-telemetry-sync:46` | None of the e1cd3096 egress fixes is pinned by a test: no test asserts gstack-telemetry-sync exits 0 on tier=anonymous, that security.ts hashes urlDomain before the telemetry spawn, or that braintrust noSendLogs requires opts.upload — a silent revert would pass CI (test/telemetry-integrity-regression.test.ts and telemetry-repo-strip.test.ts cover logger integrity and repo-stripping only) | Add three cheap static/behavioral tripwires in the existing telemetry test files, matching this repo's established pin-the-wiring pattern (hermetic-wiring, cdp-session-cleanup) |
| FYI | Egress/privacy audit: setup + installer + runtime bundle (setup, bin/gstack, runtime/, retained bin helpers, gstack-upgrade/) | `supabase/config.sh:7` | The default telemetry/community destination (https://frugpmstpnojnhfyimgv.supabase.co + publishable anon key) is baked into every install, so three retained helpers (telemetry-sync, update-check ping, community-dashboard) have a working egress destination out of the box — correctly gated today (tier defaults off, RLS documented), recorded here for the retained-tool egress ledger required by STATUS.md gate (c). | No change; keep the tier gate as the single control point and cover it with the anonymous-tier regression test from the P1 fix. |
| FYI | Triage of untracked docs/evidence on gstack2-integration | `test/platform-bakeoff.test.ts:1` | Remaining untracked test/lib files (model-benchmark, gemini adapter, telemetry regression, gbrain-provider-choice) appear to be the branch's real work, paired with the evidence dirs above | Commit alongside their evidence; ensure each is classified in E2E tiers/touchfiles if E2E |
| FYI | Untracked-directory junk audit (outputs/, .deepeval/, .tmp-ios-ab-evaluator-final/, .claude/) | — | Exact .gitignore block to add: `/outputs/`, `.claude/worktrees/`, `.deepeval/`, `.tmp-*/` | Add these four lines (after salvaging per findings above); no changes to browse/ or make-pdf paths needed |

## Docs & tracking (26)

| Sev | Area | Location | Summary | Suggested fix |
|---|---|---|---|---|
| P2 | size-claim verification: GStack 2 (skills/) vs GStack 1 skill surface + per-invocation context cost | `docs/gstack-2/BLOAT-LEDGER.json:61` | BLOAT-LEDGER.json guardrail text is stale: 'generator-preamble growth (multiplies x5), already pinned by skill-size-budget tests' — the generator no longer exists and no test pins skills/ sizes. | Re-base the ledger's guardrail sentence after the apparatus removal; point it at whatever new v2 budget test lands (finding above), otherwise the ledger claims protection that is gone. |
| P2 | eager-context-injection | `docs/gstack-2/BLOAT-LEDGER.json:20` | BLOAT-LEDGER safe cuts measured 2026-07-28 are still unapplied: make-pdf SKILL.md keeps Common flags (line 162) and 'When Claude should run it' (line 202) inline (~760 tokens), and the five dispatchers keep the duplicate Match-the-user's-language clause in step 7 (~40 tokens x5). | Apply the three ledger-approved 'risk: safe' cuts (move make-pdf flags/debugging/output-contract to references/make-pdf-usage.md, drop the dup trigger list, drop the dup language clause) via the generator inputs, then regenerate. |
| P2 | complaint-coverage audit, slice 0/10 of docs/gstack-2/BACKLOG-MAP.json (items at index % 10 == 0; 76 items, 21 sampled) | `setup-gbrain/SKILL.md.tmpl:690` | garrytan/gstack#2250 (idx 720, FIX_IN_GSTACK_2) is still broken in the retained 1.x path: setup-gbrain/SKILL.md.tmpl:690 and setup-gbrain/memory.md invoke extensionless bin/gstack-memory-ingest and gstack-gbrain-sync, but bin/ ships only the .ts files, reproducing the 'script missing at install root' failure on every host. | Either fix the invocations to the .ts entrypoints (or add extensionless launchers) or re-disposition to SUPERSEDED_BY_CONSOLIDATION with the gstack-code-intelligence provider path as the replacement; today the disposition claims a fix that does not exist. |
| P2 | complaint-coverage audit, slice 2/10 (BACKLOG-MAP index % 10 == 2) | `docs/gstack-2/BACKLOG-MAP.json` | garrytan/gstack#1058 (FIX_IN_GSTACK_2, idx 182) unimplemented and likely mis-dispositioned: no assumption-verification layer exists anywhere under skills/plan, and shipping /avl as a new layer would violate the six-top-level-modes contract — the disposition promises a fix the architecture forbids as scoped | Re-disposition to NEEDS_EVIDENCE/SUPERSEDED, or fold assumption-verification pressure into an existing plan mode's question content |
| P2 | complaint-coverage audit, slice 5/10 of docs/gstack-2/BACKLOG-MAP.json (items idx % 10 == 5) | `/Users/sinabina/gstack/docs/gstack-2/BACKLOG-MAP.json:1` | garrytan/gstack#1412 (/plan-pm-review + /pm 'seventh role') is dispositioned FIX_IN_GSTACK_2 ('clear open defect to solve'), but no PM-role content exists anywhere in skills/ and the canonical contract freezes /plan at exactly six top-level modes — implementing this disposition would violate the contract and add a feature-request as a mandated fix | Re-disposition #1412 to NEEDS_EVIDENCE, DEFER_COMMUNITY, or NOT_CORE; FIX_IN_GSTACK_2 on a new-skill proposal contradicts both the six-mode contract and the no-features-nobody-asked-for goal |
| P2 | complaint-coverage audit, slice 5/10 of docs/gstack-2/BACKLOG-MAP.json (items idx % 10 == 5) | `/Users/sinabina/gstack/docs/gstack-2/BACKLOG-MAP.json:1` | garrytan/gstack#1174 (Conductor template build exit 128; FIX_IN_GSTACK_2) has no fix trace anywhere in the tree and the item's own metadata contradicts the disposition: family 'out-of-scope-or-insufficient-signal', component docs-governance — a 'clear open defect to solve in the consolidated architecture' that nothing in the architecture addresses | Re-disposition #1174 to NEEDS_EVIDENCE or NOT_CORE (Conductor's template is an external artifact), or attach the actual fix evidence |
| P2 | complaint-coverage audit, slice 6/10 of docs/gstack-2/BACKLOG-MAP.json (items with index % 10 == 6; 24 of 75 sampled) | `skills/plan/references/legacy/learn.md:209` | garrytan/gstack#2030 (PORT_JUDGMENT, half-ported): the signal-gated-capture half is ported (learn.md:202-210 marker pr=2030), but the helpful/harmful-counter half is a single unactionable prose sentence — bin/gstack-learnings-feedback (added by the PR) does not exist and gstack-learnings-log/-search contain zero counter support, so the ported instruction 'Track helpful and harmful outcomes separately' has no mechanism. | Either port the feedback counter fields/CLI into the runtime bundle or delete the counter sentence so the ported judgment matches what the system can actually do. |
| P2 | Complaint-coverage audit, slice 8/10 of docs/gstack-2/BACKLOG-MAP.json (75 items, 26 sampled) | `lib/gstack-memory-helpers.ts:110` | garrytan/gstack#1895 (item idx 478) still reproducible: canonicalizeRemote strips `.git` BEFORE the trailing slash (lib/gstack-memory-helpers.ts:110-112), so 'https://github.com/foo/bar.git/' canonicalizes to 'github.com/foo/bar.git' — the .git-suffix key split the issue complained about is live | Swap the two replace calls (strip trailing slashes first, then .git) and add the trailing-slash case to the function's examples/tests |
| P2 | Complaint-coverage audit, slice 8/10 of docs/gstack-2/BACKLOG-MAP.json (75 items, 26 sampled) | `docs/gstack-2/BACKLOG-MAP.json:1` | All 60 NEEDS_EVIDENCE items in this slice point at replacementTest contract IDs (installation.host-matrix-contract, runtime.browser.lifecycle-contract, docs.generated-source-contract, judgment.design.contract, etc.) that exist nowhere in the current tree — only the review precision contract has real tests (test/skill-e2e-review-precision.test.ts); the map's own generator (scripts/gstack2/generate-backlog-map.ts) is also deleted, so the map can neither be verified nor regenerated | Either check in a thin contract-ID → surviving-test index (one JSON/MD table) or amend BACKLOG-MAP's methodology note to state contracts are documentation-only post-refactor; also note 8 slice items route to judgment.design.contract for the retired /design surface |
| P2 | Triage of untracked docs/evidence on gstack2-integration | `docs/gstack-2/BLOAT-LEDGER.json:1` | docs/gstack-2/BLOAT-LEDGER.json is a dated bloat measurement with actionable safeCuts, self-consistent with the no-bloat goal, but unreferenced by STATUS.md | Commit and link from STATUS.md; note its safeCuts (e.g. ~760 tokens off make-pdf SKILL.md) are still-open work items, not done |
| P2 | Triage of untracked docs/evidence on gstack2-integration | `docs/gstack-2/BROWSER-INFRASTRUCTURE-BEFORE-AFTER.md:1` | docs/gstack-2/BROWSER-INFRASTRUCTURE-BEFORE-AFTER.md is a candidate-vs-1.x comparison that correctly defers release claims to STATUS.md, but nothing links back to it | Commit-worthy evidence; add a link from STATUS.md so it is discoverable, and re-verify the pinned commits (a84a6e23, 75b35766) exist on the branch |
| FYI | skills/review/ (GStack 2 review dispatcher package) | `docs/gstack-2/BLOAT-LEDGER.json:1` | Eager per-invocation cost is healthy and already measured: review ~2,981 tokens including the 1,699-token forced triad (EXECUTION-PROFILES + SHARED-JUDGMENT + AUTHORITY-POLICY read every invocation); making the triad lane-specific is ledgered as the biggest lever, deferred pending A/B evidence. | No new action — my measurements match the ledger; keep the triad decision behind A/B win-rate as planned. |
| FYI | skills/make-pdf end-to-end audit (size, laziness, drift, bloat, hardcoding) | `docs/gstack-2/BLOAT-LEDGER.json:1` | BLOAT-LEDGER.json safeCuts target 'make-pdf/SKILL.md.tmpl' (move Common flags/Debugging/Output contract to a lazy reference, ~760 tokens), but that .tmpl no longer exists after the generator removal and the cuts conflict with the make-pdf stay-as-is mandate. | Mark those ledger entries stale/blocked so a future fix agent does not act on them against the mandate. |
| FYI | size-claim verification: GStack 2 (skills/) vs GStack 1 skill surface + per-invocation context cost | `docs/gstack-2/BLOAT-LEDGER.json:5` | BLOAT-LEDGER eager numbers verified byte-exact against disk: triad = 6,796 B = 1,699 tok; plan 17,027 B/4,257 tok, ship 13,263/3,316, qa 12,706/3,177, review 11,923/2,981, debug 11,883/2,971, make-pdf 9,742/2,436 (no triad) — all match within 1 token. | No action; the ledger's per-invocation instrumentation is trustworthy and can be cited directly in release copy. |
| FYI | complaint-coverage audit, slice 2/10 (BACKLOG-MAP index % 10 == 2) | `docs/gstack-2/BACKLOG-MAP.json` | garrytan/gstack#368 (idx 22) and #203 (idx 2) are stale NEEDS_EVIDENCE: the exact complaint GStack 2 exists to kill (browse docs / full context loaded on every invocation) is already solved — dispatchers total 617 lines (qa is 74) with browse docs behind lazy references — yet the map says these 'require repro before implementation' | Re-disposition both to SUPERSEDED_BY_CONSOLIDATION so the ledger records the headline complaint class as closed |
| FYI | complaint-coverage audit, slice 4/10 (BACKLOG-MAP items index % 10 == 4; 76 items, 24 sampled/verified) | `docs/gstack-2/BACKLOG-MAP.json:33035` | garrytan/gstack#2243 (idx 714, NEEDS_EVIDENCE 'add opus-4-8 overlay + resolver mapping') is stale: the entire model-overlay apparatus (model-overlays/, scripts/models.ts, overlay fixtures) is deleted on this branch, so there is nothing to collect evidence for. | Re-disposition to SUPERSEDED_BY_CONSOLIDATION so the map reflects the intentional overlay-apparatus removal instead of implying a pending product decision. |
| FYI | complaint-coverage audit, slice 5/10 of docs/gstack-2/BACKLOG-MAP.json (items idx % 10 == 5) | `/Users/sinabina/gstack/docs/gstack-2/BACKLOG-MAP.json:1` | garrytan/gstack#1048 (plan-review 'minimal diff' bias — a judgment-calibration complaint about plan-eng/ceo-review) is classified component=runtime-context-dev with replacementTest runtime.context-isolation-contract, so its coverage would be asserted by a state-isolation test that cannot exercise reviewer bias | Reclassify to component=plan / judgment.plan.contract so any future evidence work tests the right surface |
| FYI | complaint-coverage audit, slice 5/10 of docs/gstack-2/BACKLOG-MAP.json (items idx % 10 == 5) | `/Users/sinabina/gstack/docs/gstack-2/BACKLOG-MAP.json:1` | garrytan/gstack#984 (browse cannot authenticate with Rails+Devise+Turbo apps; FIX_IN_GSTACK_2) has no verifiable fix trace — no issue-number comment, test, or doc row ties current browse auth/cookie code to this defect (unverified, not disproven) | Attach a repro fixture or evidence pointer for #984, or downgrade to NEEDS_EVIDENCE like its unproven siblings |
| FYI | complaint-coverage audit, slice 6/10 of docs/gstack-2/BACKLOG-MAP.json (items with index % 10 == 6; 24 of 75 sampled) | `docs/gstack-2/BACKLOG-MAP.json:1` | garrytan/gstack#2245 (NEEDS_EVIDENCE, stale): the PR adds a gpt-5.6 model overlay, but the entire model-overlay apparatus (model-overlays/, scripts/models.ts, overlay fixtures) is deleted in this branch — a disposition implying pending evidence/implementation against a removed subsystem should be SUPERSEDED_BY_CONSOLIDATION. | Re-disposition #2245 (and sibling overlay items) to SUPERSEDED_BY_CONSOLIDATION so the map doesn't imply future overlay work. |
| FYI | complaint-coverage audit, slice 6/10 of docs/gstack-2/BACKLOG-MAP.json (items with index % 10 == 6; 24 of 75 sampled) | `docs/gstack-2/BACKLOG-MAP.json:1` | garrytan/gstack#604 (NEEDS_EVIDENCE, stale): the Revyl cloud-device + Appium mobile-QA PR is mapped as needing a product decision, but the canonical contract already forbids cloud browsers and alternate iOS drivers ('Physical iOS uses only DebugBridge/CoreDevice'), so the decision is made and the disposition should be a recorded rejection, not pending evidence. | Re-disposition #604 to NOT_CORE/rejected citing the contract clause, so nobody re-litigates cloud devices. |
| FYI | complaint-coverage audit, slice 6/10 of docs/gstack-2/BACKLOG-MAP.json (items with index % 10 == 6; 24 of 75 sampled) | `docs/gstack-2/BACKLOG-MAP.json:1` | garrytan/gstack#723 and #1206 (both NEEDS_EVIDENCE, stale): both complaints are already resolved by GStack 2 itself — the five workflow-position dispatchers answer #723, and installer `--skill` selection (proven in the STATUS.md Codex runtime-absent run) answers #1206 — but the map still marks them unverified instead of pointing at that evidence. | Re-disposition both to SUPERSEDED_BY_CONSOLIDATION with links to the dispatcher tree and install-matrix artifacts. |
| FYI | complaint-coverage slice 7/10 (BACKLOG-MAP index % 10 == 7, 25 of 75 items sampled) | `docs/gstack-2/BACKLOG-MAP.json` | garrytan/gstack#2246 disposition stale: NEEDS_EVIDENCE on a PR that adds a fable-5 model overlay, but the entire model-overlays apparatus is intentionally deleted on this branch — leaving it open invites re-adding removed scaffolding | Re-disposition #2246 to SUPERSEDED_BY_CONSOLIDATION |
| FYI | Complaint-coverage audit, slice 8/10 of docs/gstack-2/BACKLOG-MAP.json (75 items, 26 sampled) | `docs/gstack-2/BACKLOG-MAP.json:1` | Slice FIX_IN_GSTACK_2 items #2032 (design --count validation, idx 578) and #1159 (DESIGN_NOT_AVAILABLE fallback, idx 228) are resolved by removal — the design/ binary and the DESIGN_NOT_AVAILABLE mechanism no longer exist anywhere in the tree; #957 (OPENAI_BASE_URL, idx 158) is likewise moot since no in-tree code calls the OpenAI API directly anymore | Acceptable outcome (complaint killed, not patched); record removal-supersession in any map refresh so these don't look unimplemented. Verified-good in the same pass: #1071 port present, #1611/#1677/#1723/#1836/#1882/#1947/#1974/#2009 fixed, #2201 consolidation holds (single .compat/open-gstack-browser alias), #2187 correctly deferred, time-attack#2 moot (basic-ftp absent from dep graph) |
| FYI | Complaint-coverage audit, slice 9/10 (BACKLOG-MAP items with index % 10 == 9; 75 items in slice, 24 sampled and verified against the current tree) | `docs/gstack-2/BACKLOG-MAP.json` | Item time-attack/gstack#3 (idx 749) disposition RETAIN_INTERNAL is stale: the file it patches, scripts/gen-skill-docs.ts, was deleted with the generation apparatus (commit 3448905e), so there is no internal baseline retaining this work. | Reclassify to SUPERSEDED_BY_CONSOLIDATION on the next map regeneration; no code change needed. |
| FYI | Complaint-coverage audit, slice 9/10 (BACKLOG-MAP items with index % 10 == 9; 75 items in slice, 24 sampled and verified against the current tree) | `docs/gstack-2/BACKLOG-MAP.json` | Items garrytan/gstack#1497 (idx 309, DEFER_COMMUNITY: pi host) and #1072 (idx 189, NEEDS_EVIDENCE: Kimi CLI) are already satisfied — STATUS's 510/510 install matrix shows Pi and Kimi Code CLI passing via the standard installer with no host-specific code, exactly the intended architecture. | Mark both as superseded-by-standard-installer on next map regeneration so they don't resurface as open community asks. |
| FYI | Triage of untracked docs/evidence on gstack2-integration | `.github/dependabot.yml:1` | .github/dependabot.yml is a small standard weekly-update config (bun + actions, grouped); harmless, no context cost to skills | Commit-worthy; unrelated to GStack 2 evidence so land it as its own commit |

## Tests & evidence (21)

| Sev | Area | Location | Summary | Suggested fix |
|---|---|---|---|---|
| P2 | Host coverage audit — codex (feeds STATUS.md P0 gate (a) broad per-host UI launch coverage) | `evals/host-adversarial/fixtures:1` | No live codex coverage exists for the plan dispatcher: adversarial fixtures cover only debug/qa/review/ship and the runtime-absent run covered qa, so 'five-dispatcher UI launch' is unproven for plan on this host. | Minimal live step to close codex: restore the harness, then run one paid one-shot (GSTACK_RUN_CODEX_HOST_ADVERSARIAL=1 bun run scripts/gstack2/host-adversarial.ts --model gpt-5.4, i.e. codex exec --json --ephemeral --ignore-user-config --ignore-rules -s read-only) against the current tree with the four fixtures plus a new plan-launch fixture, committing the artifact regardless of outcome. |
| P2 | Host coverage audit — codex (feeds STATUS.md P0 gate (a) broad per-host UI launch coverage) | `evals/host-adversarial/README.md:3` | evals/host-adversarial/README.md header is stale: 'v1 FAILED; immutable v2 FAILED; live v3 NOT RUN. No passing raw-prompt installed-host behavioral run has been recorded' contradicts the committed passing run in its own runs/ directory. | Rewrite the status header to record both 07-22 v3 one-shots (fail then 4/4 pass) and the v4 failure, keeping the no-relabel discipline. |
| P2 | Host coverage audit: cursor (P0 gate (a) — per-host UI launch coverage) | `evals/browser-provider-evals/2026-07-20-browser-provider-eval.md:67` | The only existing live Cursor session (Cursor Agent 2026.07.16-899851b, PASS WITH CONTRACT AMBIGUITY on browser-provider journeys) loaded the legacy clone path `.agents/skills/gstack/qa/SKILL.md`, which that eval's own P0 finding disqualifies — it is supplemental evidence and cannot be relabeled to close gate (a). | Keep it supplemental-only; require the new live run's artifact to record the loaded SKILL.md path and prove it is the canonical `.agents/skills/qa/SKILL.md`, then optionally cite this eval from the Cursor row as supplemental (it is currently uncited in HOST-COMPATIBILITY.md). |
| P2 | Host coverage audit: openclaw (feeds P0 gate (a) broad per-host UI launch coverage) | `test/openclaw-native-skills.test.ts:7` | Legacy ClawHub-published skills (openclaw/skills/gstack-openclaw-{ceo-review,investigate,office-hours,retro} plus test/openclaw-native-skills.test.ts) form a parallel OpenClaw judgment surface under retired names, which per the gstack2 contract are compat-only; they can drift from canonical modules and muddy host-coverage claims. | Decide their disposition before closing the gate: republish them as thin pointers to the canonical five, or explicitly scope them out of gstack2 host-coverage claims in HOST-COMPATIBILITY.md; do not count them as OpenClaw coverage evidence. |
| P2 | Host coverage audit: GitHub Copilot (P0 gate a — per-host UI launch) | `skills/.compat/qa-only/SKILL.md:1` | 44 compat aliases now exist as real SKILL.md files under skills/.compat/ (metadata.internal: true), but HOST-COMPATIBILITY.md still claims compat files 'are not SKILL.md files'; default-discovery exclusion under this layout is unproven for copilot installs. | In the same live run, assert the --list count is exactly 6 and that no .compat alias lands in .agents/skills; update the doc sentence about compat file format. |
| P2 | complaint-coverage audit, slice 4/10 (BACKLOG-MAP items index % 10 == 4; 76 items, 24 sampled/verified) | `evals/installation/install-matrix.json:1` | garrytan/gstack#661 (idx 94, FIX_IN_GSTACK_2, replacementTest installation.host-matrix-contract): Factory/Droid skill discovery has zero coverage — no factory/droid mention in evals/installation/install-matrix.json (cursor/kimi/etc. all present) nor in docs/gstack-2/HOST-COMPATIBILITY.md. Fix neither implemented nor evidenced. | Either add a Factory row to the standard-installer matrix evidence (proving discovery works via `npx skills add`) or re-disposition to SUPERSEDED_BY_CONSOLIDATION with an explicit note that host placement is delegated to the skills CLI. |
| P2 | Triage of 7 untracked new test/lib files on gstack2-integration | `test/model-benchmark-evaluation.test.ts:2` | test/model-benchmark-evaluation.test.ts passes (2/2, 60ms, no API key) but is bloat: its only purpose is pinning the superseded in-house evaluation.ts scorer. | Delete alongside lib/model-benchmark/evaluation.ts. If somehow evaluation.ts is kept, this would be gate tier — but the right call is deleting both. |
| P2 | Triage of untracked docs/evidence on gstack2-integration | `evals/host-adversarial/runs/2026-07-21-v4-live-gpt-5-6-sol.json:1` | evals/host-adversarial/runs/2026-07-21-v4-live-gpt-5-6-sol.json is a retained FAILED live run, unreferenced in STATUS or the runs README (siblings are tracked) | Commit it (matches the explicit retained-without-relabeling evidence policy) and add a one-line entry to evals/host-adversarial/runs/README.md |
| P2 | Triage of untracked docs/evidence on gstack2-integration | `evals/browser-provider-evals/2026-07-20-mac-mini-production-eval.md:1` | evals/browser-provider-evals/2026-07-20-mac-mini-production-eval.md is a NOT-READY production eval; sibling eval is tracked but STATUS/TEST-EVIDENCE never cite this file | Commit as evidence and cite it from STATUS.md where browser-provider readiness is claimed, or the NOT-READY finding stays invisible to release gating |
| P2 | Triage of untracked docs/evidence on gstack2-integration | `evals/platform-bakeoff/README.md:1` | evals/platform-bakeoff/ is reproducible research evidence (README says retained, not runtime) with a matching untracked test/platform-bakeoff.test.ts that references it | Commit harness + test together (they are a unit) or drop both; check results/ subdir for bulky raw output before committing |
| P2 | Truth-audit of docs/gstack-2/TEST-EVIDENCE.md (artifact existence, package.json command validity, deleted model-overlay apparatus references) on gstack2-integration | `CONTRIBUTING.md:579` | Two docs still describe the deleted scripts/preflight-agent-sdk.ts as a live conductor-env-shim import site: CONTRIBUTING.md:579 and USING_GBRAIN_WITH_GSTACK.md:270. | Drop preflight-agent-sdk.ts from both import-site lists (CHANGELOG/TODOS mentions are historical and fine; docs/gstack-2/SKILL-CONTENT-CHECKLIST/README.md:75 correctly records the deletion as an action, not a live reference). |
| FYI | Host coverage audit — codex (feeds STATUS.md P0 gate (a) broad per-host UI launch coverage) | `skills/make-pdf:1` | skills/make-pdf lacks agents/openai.yaml while all five dispatchers ship one, so codex/OpenAI presentation metadata is inconsistent for the sixth canonical skill. | Decide whether tool skills intentionally skip host presentation metadata; if not, have the generator emit make-pdf's openai.yaml for consistency. |
| FYI | Host coverage audit — codex (feeds STATUS.md P0 gate (a) broad per-host UI launch coverage) | `skills/.compat/codex/SKILL.md:1` | Offline checks that PASS for codex right now: installer targets .agents/skills at both scopes with hash-verified copies (4 green matrix cases incl. selected-subset and explicit alias); .compat/codex alias is thin, judgment-free, routes to $review --mode Deep --module codex with the correct single-skill install instruction; runtime-absent artifact shows judgment-only behavior with zero workspace mutation. | No action; retain as the installer-layer + judgment-boundary baseline that the single fresh live run then re-anchors to the current tree. |
| FYI | Host coverage audit: kimi (Kimi Code CLI) — P0 gate (a) per-host UI launch | `evals/installation/install-matrix.json:1757` | Positive offline verification done now: installed the current canonical tree via skills@1.5.19 for agent kimi-code-cli into a temp project, then ran kimi 1.49.0's own discovery code (find_project_skills_dirs + discover_skills_from_roots from /Users/sinabina/.local/share/uv/tools/kimi-cli/.../kimi_cli/skill/__init__.py) — all six skills (debug, make-pdf, plan, qa, review, ship) discovered at project scope with correct names/descriptions; no builtin name collision (kimi builtins are only kimi-cli-help and skill-creator). | This closes 'does kimi actually read the installer's path' without a paid run; cite it as supporting (not substitute) evidence — the gate still requires the live agent-process invocation. |
| FYI | Host coverage audit: kimi (Kimi Code CLI) — P0 gate (a) per-host UI launch | `outputs/provider-installation/kimi-agent/result.json:1` | Minor kimi installer quirks recorded only in untracked outputs/: leading 'codex Agent detected' banner while explicitly targeting kimi (cosmetic, --agent flag wins), stale skills-lock.json entries after removal (KIMI-INSTALL-002), and multi-name --skill selection triggering a 113-skill full-depth scan. | Fold these as caveat lines into the kimi row or TEST-EVIDENCE.md when committing the rerun artifact; none block the gate. |
| FYI | Host coverage audit: cursor (P0 gate (a) — per-host UI launch coverage) | `skills/qa/SKILL.md:2` | Offline verification results (all pass): six SKILL.md files carry only portable name/description frontmatter (no host-specific keys, Portable tier holds for Cursor); no cursor install logic remains in the repo (hosts/cursor.ts deleted, only hosts/claude/hooks survives — contract-compliant); skills/*/references/BROWSER-PROVIDERS.md now contains the 'Cursor interactive browser provider' section plus formal needs-user-action/ready states, closing the 2026-07-20 eval's P1 registry and readiness gaps; cursor removal case is green in the matrix. | No action beyond the live step; note that agents/openai.yaml presentation metadata rides along into every cursor install (5 skills) — harmless per the Native-adapters policy and tiny, but it is dead weight for non-Codex hosts if bloat trimming continues. |
| FYI | host-coverage: pi | `evals/installation/install-matrix.json:1` | In the committed artifact the skills-CLI banner prints 'codex Agent detected — installing non-interactively' during Pi installs even though `--agent pi` was honored (files landed and hash-verified at Pi roots). | No action needed; note it so future auditors reading the raw stdout don't misclassify the Pi cases as Codex installs. |
| FYI | Host coverage audit: openclaw (feeds P0 gate (a) broad per-host UI launch coverage) | `skills/qa/SKILL.md:1` | Offline portability verification PASSES today: all six skills/*/SKILL.md frontmatters are spec-clean (name + description only, names match directories), dispatcher bodies have zero Claude-specific tool dependencies (AskUserQuestion / ~/.claude / claude -p all absent), and the recorded matrix confirms OpenClaw targets project skills/ and global ~/.openclaw/skills with hash-verified physical copies. | No action needed on skill content for OpenClaw; the remaining work is evidence (live run + matrix re-run), not portability fixes. |
| FYI | Host coverage audit: GitHub Copilot (P0 gate a — per-host UI launch) | `hosts/claude/hooks/question-log-hook.ts:1` | hosts/ contains no copilot config (only hosts/claude/hooks) — correct per the canonical contract that the standard installer owns host placement; no obsolete copilot scaffolding to remove. | None; offline portable check also passed: exactly six public SKILL.md files with spec-conformant name/description frontmatter in skills/. |
| FYI | Host coverage audit: GitHub Copilot (P0 gate a — per-host UI launch) | `evals/installation/install-matrix.json:2813` | Artifact stdout for both copilot installs shows a 'codex Agent detected' banner despite --agent github-copilot; placement and summary correctly targeted GitHub Copilot. | Cosmetic upstream skills-CLI display quirk; verify the banner in the fresh run and ignore unless placement is wrong. |
| FYI | Triage of 7 untracked new test/lib files on gstack2-integration | `test/helpers/touchfiles.ts` | None of the five new tests need a paid API key — all are local/deterministic (template reads, JSON fixtures, local bash bin spawns), so none belong in the paid E2E tiering (E2E_TIERS/touchfiles.ts); every kept one lands in the free `bun test` gate automatically. | No touchfiles.ts entries needed for any kept file; do not classify them in E2E_TIERS. |

