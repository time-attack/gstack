# SiteRent

Construction site equipment rental management. Django 4.2 + Django REST
Framework + PostgreSQL.

Apps:

- `rentals` — sites, equipment rentals, rental lifecycle.
- `billing` — invoices, ledger entries, refunds (added in the billing branch).

Run tests with `pytest`.
